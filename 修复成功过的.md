# 修复成功过的

整理时间：2026-06-16

## 结论

截至当前记录，没有证据证明“ISRO 返回功能完整修复成功过”。

完整成功必须同时满足：

- ISRO 返回 UI 正确显示。
- `移动至队员位置` 可用，且至少 2 人组队才允许传送。
- `移动至使用者指定的位置` 可用。
- GS 和 EXE 链路都闭合。
- 右键返回卷轴第一次、第二次都不闪退。
- 对应时间段 FatalLog 和客户端 Dump 无新增异常。

下面只保留“确实成功过的阶段性工作”，不再把失败补丁写成成功。

## 1. 成功提取过关键 UI 分支

- CSRO 当前真实三选项分支：`0x12E3464`。
- CSRO 五选项分支：`0x12E39EF`。
- ISRO 五选项参考分支：`0x722C4F`。
- CSRO jump table：`0x12E956C`。
- 已确认五选项分支里存在目标按钮字符串：
  - `UIIT_CLT_REVERSE_RETRUN_SCROLL_PARTY_MOVE`
  - `UIIT_CLT_REVERSE_RETRUN_SCROLL_USER_APPOINT`
  - `UIIT_CLT_REVERSE_RETRUN_SCROLL_TO_APPOINT_CHANGE`

成功含义：分支和字符串定位成功。

不代表：UI 已经可达，功能已经修好。

## 2. 成功抓到真实运行入口

- Frida 成功挂到：
  - `SetMsgBoxHandler`：`0x70DF10`
  - `.rret` dispatch：`0x12DFA80`
  - 五选项分支：`0x12E39EF`
  - 生命周期相关点：`0x12E35A6` / `0x12EB3C0`
- 真实右键返回卷轴参数为 `arg0=33`，即 `0x21`。

成功含义：解释了之前界面不变的根因。

不代表：已经完成修复。

## 3. 成功回滚过 Round12 失败补丁

- 失败补丁：`patch-manifest-round12-type2a2b-ui-guard-20260614-105116.json`
- 回滚证据：`rollback-round12-failed-ui-guard-20260614-110413.json`
- 成功点：
  - 只回滚 Round12 的两处范围。
  - 未动 GS、DB、资源、DLL、强化、界限突破石、防爆石。

成功含义：失败补丁定点回滚成功。

不代表：返回功能修复成功。

## 4. 成功回滚过 v11/v12 失败范围

- 回滚记录：`client_rollback_failed_v11_v12_return_patches_20260612.md`
- 回滚范围：
  - `0x12DFA61`, size `0x6`
  - `0x12EB600`, size `0x300`
  - `0x12EB900`, size `0x100`
- 成功点：
  - 只回滚失败的客户端返回 UI 补丁。
  - GS 未动。
  - 没有回滚其它功能补丁。

成功含义：失败 UI 补丁边界清理成功。

不代表：客户端返回 UI 修好。

## 5. 成功执行过“只回滚返回补丁”

- 回滚证据：`rollback-return-only-from-pre-return-state-20260614-120737.json`
- 成功点：
  - 没有整文件恢复。
  - 只按返回相关 manifest 范围恢复。
  - EXE 处理 20 组返回相关范围。
  - GS 处理 5 组返回相关范围。
  - 记录 `changed_count=23`、`skipped_count=4`。
  - 未用 `I:\SRO_Client.exe` 直接覆盖当前 EXE。

成功含义：按用户铁律清掉失败返回补丁。

不代表：返回功能已经添加成功。

## 6. 后续可继续使用的正确证据

- 真实入口必须从 `0x21` 入手。
- UI 不能只接五选项分支，必须接完整生命周期。
- 后续修复必须同时闭合：
  - `0x759F` / `0x7600` 列表请求
  - `0x3213` 队员位置移动
  - `0x705A` 使用者指定位置移动
  - GS 对队伍人数、队员状态、保存位置、返回物品白名单的校验
  - 第二次右键打开/关闭生命周期

## 7. Phase4 事件表只算静态闭合，游戏内失败

- 时间：2026-06-16。
- 补丁记录：`C:\Users\43584\Documents\添加ISRO的返回功能\evidence\patch-manifest-isro-return-phase4-event-map-20260616-105953.json`
- 本轮备份：`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase4-event-map-20260616-105953`
- 写后 hash：
  - EXE：`AC4C59324D9C501968C014EBDE60D9B7ADF907D048F1ED3883E30C019A35B2C1`
  - GS：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`
- 静态验证：
  - `check_reverse_return_event_map_current.py`：8/8 pass。
  - `check_reverse_return_real_ui_entry.py`：7/7 pass。
  - `deep_reverse_chain_check.py --targets`：11/11 pass。
- 静态含义：可达 message-map 曾指向 `0x12ED000`，可达事件表包含 `0x102/0xCC/0xCF`，并且 handler 为 `0x12DF590/0x12DEF50/0x12DF020`；Phase2 slot guard、原 static initializer、旧 lifecycle guard 禁止路线均保持正确。
- 游戏内结果：失败。用户右键返回卷轴产生客户端 dump `F:\CSRO客户端\Dump\1N[2026-06-16 11-14-54]_30_34 谁与争锋[西南]_ak48.dmp`。
- 结论：Phase4 不算返回功能修复成功，只能作为“静态检查曾闭合但游戏失败”的反例；已按本轮备份回滚。
## 2026-06-16 15:35 - ISRO 返回 UI 界面添加成功的方法

本次已确认成功的只限于 UI 显示层，不代表功能链路全部成功。

- 真实右键入口仍然是 `SetMsgBoxHandler` 的 `arg0=0x21`，不能再从 `0x2A/0x2B` 入手。
- 成功显示 ISRO 样式返回界面的关键做法是：
  - `0x007BAAD9` 右键返回卷轴调用跳到 `0x012EC930` field bridge stub。
  - stub 只处理 `0x21/0x25/0x28`，并把 CSRO 小返回框字段桥接到五/六按钮 UI 需要的字段。
  - stub 在进入 `0x012DFA80` 前补齐关键字段：保存 item slot 到 `0x012ECFF0`，桥接 `[ecx+0x808]/[ecx+0x80C]/[ecx+0x810]/[ecx+0x854]`，避免旧的空 `[esi+0x810]` 崩溃。
  - `0x21` 和 `0x25` 的大分发表项指向 `0x012E39EF`，由 CSRO 现有 `.rret` 五/六按钮分支创建 ISRO 返回 UI。
- 这套方法的成功边界：界面能显示出 ISRO 风格的返回按钮；它不证明旧功能、新增功能、服务端链路已经全部正确。

## 2026-06-16 20:04 - Phase8 小扩展 message-map 静态准入成功

本次只确认“补丁方法静态准入成功”，不代表游戏内最终成功。

- 写前冻结：
  - EXE：`4F07FABFEC26C3AAEFEF7BB3B623EEF95306DE6B7A837AEE7076D3AF873E88B8`
  - GS：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`
- 写后：
  - EXE：`56C67B783B00CC4F7BBA7015A0C7692FF334C1C8C0A2A98E3EB3A4593C8F1D33`
  - GS：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`
- 本轮整文件备份：
  - `C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase8-base-chain-extension-20260616-200403`
- manifest：
  - `C:\Users\43584\Documents\添加ISRO的返回功能\evidence\patch-manifest-isro-return-phase8-base-chain-extension-20260616-200403.json`
- 成功方法：
  - 保留 Phase6 UI bridge：`0x007BAAD9 -> 0x012EC930`。
  - 不再把 `0x00EBE9F8` 指到整张追加表。
  - `0x00EBE9F8` 恢复为原 CSRO entries：`0x00FFE1E8`。
  - `0x00EBE9F4` 改为小扩展 map：`0x012ED000`。
  - `0x012ED000` header 指回原 base：`0x00EC21B4`，entries 指向 `0x012ED020`。
  - `0x012ED020` 只含 `0xCC -> 0x012DEF50`、`0xCF -> 0x012DF020` 和 terminator。
  - 旧按钮 `0xC8/0xC9/0xCA/0xCB` 不进入新表，继续依赖原 CSRO runtime initializer 写回旧 handler。
- 静态检查：
  - `python tools\check_reverse_return_phase8_base_chain_extension.py --json --exe F:\CSRO客户端\SRO_Client.exe --gs D:\CSRO\8-SR_GameServer.exe`：10 pass / 0 fail。
  - `python tools\check_reverse_return_phase8_old_csro_preserved.py --json --exe F:\CSRO客户端\SRO_Client.exe --gs D:\CSRO\8-SR_GameServer.exe`：13 pass / 0 fail。
- 仍需游戏内验证：
  - 第一次右键返回卷轴不崩。
  - 第二次右键返回卷轴不崩。
  - 旧 `0xC8/0xC9/0xCA/0xCB` 功能可用。
  - `0xCC` 必须 2 人以上组队才能传送。
  - `0xCF` 不闪退。
  - `D:\CSRO\FatalLog` 和 `F:\CSRO客户端\Dump` 无同时间新增异常。
## 2026-06-16 20:33 - Phase8 记录降级：EXE-only 失败，不算成功

Phase8 的“小扩展 message-map 静态准入”只保留为思路参考，不能再写成成功阶段。

用户复核指出：`D:\CSRO\8-SR_GameServer.exe` 的 LastWriteTime 仍停在 `2026-06-16 06:51:01`。这证明 Phase8 当前轮次没有写 GS，只是 EXE-only 尝试。返回功能目标要求 EXE 与 GS 同轮闭合，因此 Phase8 不满足成功条件。

当前应保留的正确经验：
- `0x00EBE9F8` 不应再被整表替换到全量追加表。
- 旧按钮 `0xC8/0xC9/0xCA/0xCB` 不能进入新 handler，必须保持原 CSRO 链路。
- 新增链路只能面向 `0xCC/0xCF`。
- 真实入口仍以 `0x21` 为准。

当前必须删除的成功口径：
- 不能把 Phase8 称为“修复成功”。
- 不能把 Phase8 称为“EXE/GS 修复完成”。
- 不能把 GS 06:51 的旧静态检查 pass 当作本轮 GS 修复证据。

当前冻结状态：
- `F:\CSRO客户端\SRO_Client.exe` hash：`4F07FABFEC26C3AAEFEF7BB3B623EEF95306DE6B7A837AEE7076D3AF873E88B8`
- `D:\CSRO\8-SR_GameServer.exe` hash：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`
- GS LastWriteTime：`2026-06-16 06:51:01`
## 2026-06-16 22:13 - Phase9 小扩展 message-map 静态准入成功

本条只记录“静态准入成功”，不代表游戏内最终成功。

成功方法：
- 保留 UI bridge：`0x007BAAD9 -> 0x012EC930`。
- 真实返回 UI 入口仍以 `0x21` 为准，`0x21 -> 0x12E39EF`。
- `0x00EBE9F8` 恢复为原 CSRO entries：`0x00FFE1E8`。
- `0x00EBE9F4` 指向小扩展 map：`0x012ED000`。
- `0x012ED000` header 指回旧 base：`0x00EC21B4`，entries 指向 `0x012ED020`。
- `0x012ED020` 只包含 `0xCC -> 0x012DEF50`、`0xCF -> 0x012DF020`。
- 旧按钮 `0xC8/0xC9/0xCA/0xCB` 保持原 CSRO runtime slot，不进入新增表。

本轮证据：
- 备份目录：`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase9-ui-first-and-links-20260616-221347`
- manifest：`C:\Users\43584\Documents\添加ISRO的返回功能\evidence\patch-manifest-isro-return-phase9-ui-first-and-links-20260616-221347.json`
- EXE 写后 hash：`5B1767464544ED793B3289049AEE4A4FAF327EB23CFAA3871A3AC372B30D4127`
- GS hash：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`

静态验收：
- `check_reverse_return_phase9_ui_first_and_links.py`：`10 pass / 0 fail`
- `check_reverse_return_phase8_base_chain_extension.py`：`10 pass / 0 fail`
- `check_reverse_return_phase8_old_csro_preserved.py`：`13 pass / 0 fail`
- `deep_reverse_chain_check.py --targets`：`11 pass / 0 fail`

仍需游戏内验收：
- 第一次和第二次右键返回卷轴。
- 旧 CSRO 返回功能。
- `移动至队员位置` 的 2 人以上组队门槛。
- `移动至使用者指定的位置` 不闪退。
- FatalLog 和 Dump 无同时间新增异常。
