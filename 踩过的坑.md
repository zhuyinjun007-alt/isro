# 踩过的坑

整理时间：2026-06-16

## 1. 真实入口判断错

- 已证实真实右键返回卷轴进入 `SetMsgBoxHandler` 时 `arg0=33`，即 `0x21`。
- CSRO 运行分支：
  - `0x21 -> 0x12E3464`，实际三选项分支。
  - `0x25 -> 0x12E39EF`，五选项分支。
  - `0x2A -> 0x12E489C`
  - `0x2B/0x2C/0x2D -> 0x12E1962`
- 之前多轮围绕 `0x2A/0x2B/0x25/0x28` 修改，未命中真实 `0x21` 入口，所以界面没有变化。

## 2. 只看静态会误判

- 静态脚本绿，只能证明某些跳转、表项、字节存在。
- 它不能证明右键返回卷轴真实会走到五选项分支。
- 后续必须同时用运行时参数、游戏内 UI、dump、FatalLog 交叉验证。

## 3. CSRO 有五选项分支，但不可达

- CSRO 五选项分支 `0x12E39EF` 存在。
- ISRO 五选项参考分支是 `0x722C4F`。
- 按 jump table 分支边界截断后，CSRO/ISRO 的 `0x21` 都是旧三功能 UI，不含 `0xCC/0xCF` 新增按钮；CSRO `0x25 -> 0x12E39EF` 与 ISRO `0x25 -> 0x722C4F` 才是附图 2 新增两按钮 UI 的同源分支。
- 分支中能看到：
  - `UIIT_CLT_REVERSE_RETRUN_SCROLL_PARTY_MOVE`
  - `UIIT_CLT_REVERSE_RETRUN_SCROLL_USER_APPOINT`
  - `UIIT_CLT_REVERSE_RETRUN_SCROLL_TO_APPOINT_CHANGE`
- 错误点：把“字符串/分支存在”当成“UI 已接入”。实际入口、事件、发包、析构没有闭合。
- 另一个错误点：用过大的反汇编窗口扫描 `0x21`，会扫进后面的 `0x25` 分支并误判 `0x21` 已有新按钮；后续必须按 jump table 的下一个分支起点确定扫描范围。

## 4. UI 生命周期路线已失败，禁止复用

已失败路线：

- v10：按钮框出现，但第二次右键返回卷轴崩溃。
- v11：避开 `[esi+0x818]/[esi+0x81C]` 后仍崩。
- v12：只补点击事件 bridge，未解决 allocator/list 根因。
- v13：补 `EDI=8` 后仍失败，并影响旧返回和进游戏链路。

失败 dump 形态：

- `0x00425C3F` read AV
- `0x00902E4A` write to `0x00000000`
- `0x003DCCCC` / 无效 freelist 指针

结论：崩溃不是崩溃点字节坏了，而是新增返回 UI 创建、挂载、销毁污染了客户端 UI allocator/list 或对象生命周期。

## 5. 明确禁止再用的方法

- `0x711770 -> 0x12EB600`
- `CIFMessageBox` 扩容
- message-map/static-init 猜迁移
- 猜写 `[esi+0x818]` / `[esi+0x81C]`
- 单独补 button skin
- 单独补 click bridge
- 单独补 `EDI=8`
- Round6/Round9/Round10/Round11/Round12 这种继续绕 `0x2A/0x2B` 的方法

补充记录（2026-06-16）：

- 旧 `static_reverse_check.py` 的 5 个失败项要求旧路线：`SetMsgBoxHandler` type guard、`0x2A/0x2B -> 0x25`、static initializer 改到 `.rret`、`0x12EB600` 事件表和 lifecycle guard。
- 这套旧路线与前面失败记录重合，不能作为继续修复依据。
- 只允许在一一对比确认后，补“可达事件表缺少 `0xCC/0xCF`”这种最小闭合点；不得再改 static initializer，不得再改 lifecycle guard，不得再把 `0x2A/0x2B` 当真实入口。
- `0x12EB600` 当前不是空洞，含已有数据，禁止再覆盖此地址作为事件表。

## 6. Round10 / Round11 / Round12 不算成功

- Round10：加了 `[esi+0x810]` null-safe guard，但没有命中真实 `0x21` UI 入口。
- Round11：把两处静态入口 `push 0x2A` 改成 `push 0x25`，仍没解决真实运行分支。
- Round12：把 `0x2A/0x2B` 映射到 `0x25`，用户实测界面无变化，已回滚。
- 这些记录从“修复成功过的”删除，只保留为失败经验。

## 7. GS-only / EXE-only 都不完整

- 只改 EXE：UI/发包可能出现，但 GS 接收、校验、传送不完整。
- 只改 GS：客户端 UI 和请求链不走 ISRO 功能，游戏内仍看不到两个新增选项。
- 完整修复必须 GS 和 EXE 同时闭合。

## 8. DB/资源不是成功证据

- `ITEM_EVENT_REVERSE_RETURN_SCROLL`、`ITEM_QNO_WC_FERRY2_1_01`、`UIIT_*` 在资源或数据库中存在，只说明物品/文本存在。
- D 盘 SR_GameRefData、PK2 文本不能作为 GS/EXE 修复成功证据。

## 9. 误判入口要排除

- 类似 `0x746331`、`0x7464C4` 的路径曾被误判，但不属于返回卷轴五选项链路。
- 以后必须用字符串、类型、调用方、运行时参数一起确认，不能只看 message box 形态相似。

## 10. 整文件恢复会误伤其它补丁

- `D:\SR_GameServer.exe`、`I:\SRO_Client.exe` 是原始参考，不是直接覆盖目标。
- 当前 live GS/EXE 可能有其它已保留补丁。
- 正确回滚方式：按 manifest 的 raw offset / VA / bytes 只恢复失败返回补丁。

## 11. Phase4 postcheck 旧 hash 误判

- 2026-06-16 首次执行 Phase4 事件表补丁时，EXE 写后 hash 必然从 Phase3 hash 变为 Phase4 hash，但 `check_reverse_return_event_map_current.py` 仍只接受 Phase3 旧 hash。
- 这会导致 postcheck 把已经写入后的正常 hash 误判为失败，并触发本轮备份自动还原。
- 已确认自动还原后目标 hash 回到：
  - EXE：`2555859D1BE4839341B8F901D51E79CA1DBAC169E0AAC829739AF3CE22C11862`
  - GS：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`
- 后续检查必须区分写前 Phase3 hash 与写后 Phase4 hash；不能把“写后 hash 改变”当成补丁失败。

## 12. Phase4 只改可达事件表仍失败

- 2026-06-16 第二次应用 Phase4 后，静态检查全绿，但用户在游戏内右键返回卷轴直接客户端闪退。
- 失败 dump：`F:\CSRO客户端\Dump\1N[2026-06-16 11-14-54]_30_34 谁与争锋[西南]_ak48.dmp`。
- dump 证据：
  - 异常码：`EXCEPTION_ACCESS_VIOLATION`
  - 异常地址：`0x0050D597`，模块偏移 `sro_client.exe+0x10D597`
  - 读地址：`0x000007F5`
  - 线程上下文：`EIP=0x50D597`，`EBP=0x7D5`，`ESI=0x7D5`
  - 栈上可见 `0x70DF5F`，属于 `SetMsgBoxHandler` 链路。
- 崩溃链路：
  - `0x70DF10` / `SetMsgBoxHandler`
  - `0x70DF5A -> call 0x70D6A0`
  - `0x70D700 -> lea ecx, [esi+0x7D4]`
  - `0x70D70E -> call 0x70D530`
  - `0x70D565 -> call 0x50D560`
  - `0x50D597 -> mov ecx, [ebp+0x20]`，此时 `ebp=0x7D5`
- 结论：只把可达 message-map header 指到追加表 `0x12ED000` 会让右键返回卷轴进入坏对象/坏映射路径，静态全绿也不能通过。
- 已按铁律回滚本轮 Phase4：
  - 回滚备份：`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase4-event-map-20260616-105953`
  - 回滚后 EXE：`2555859D1BE4839341B8F901D51E79CA1DBAC169E0AAC829739AF3CE22C11862`
  - 回滚后 GS：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`
- 禁止继续复用 Phase4 的“追加事件表 + 改 `0xEBE9F8` 指针”路线，除非先完整证明 `SetMsgBoxHandler` 的对象映射、构造和生命周期链路已闭合。
## 2026-06-16 15:35 - UI 成功但旧功能失效/指定位置闪退的失败点

本轮用户实测结果：

- ISRO 返回 UI 已出现。
- CSRO 原来的返回功能全失效。
- 点击 `移动至使用者指定的位置` 客户端闪退，dump：`F:\CSRO客户端\Dump\1N[2026-06-16 15-27-29]_88_34 谁与争锋[西南]_ak48.dmp`。

已确认失败原因：

- Phase6 把 `0x00EBE9F8` message-map entries 指针改到追加表 `0x012ED000`。
- 追加表不仅新增 `0xCC/0xCF`，还把旧按钮 `0xC8/0xC9/0xCA/0xCB` 等也改成了新 handler，例如：
  - `0xC8 -> 0x012DEB20`
  - `0xC9 -> 0x012DE6E0`
  - `0xCC -> 0x012DEF50`
  - `0xCF -> 0x012DF020`
- 这会让旧 CSRO 功能脱离原来的 CSRO handler/协议链路，所以界面虽然像 ISRO，但旧功能不能用。

后续禁止复用的方法：

- 禁止把旧 CSRO 返回按钮统一接到 ISRO/new handler。
- 禁止用“追加整张事件表 + 全局替换 message-map 指针”当作成功修复依据。
- 禁止继续沿用“旧按钮也走 `0x012DEB20/0x012DE6E0` 等新链路”的方案。
- 新修复必须做到：UI 可以是 ISRO 样式，但旧按钮保持 CSRO 原始功能链路；只允许新增的 `移动至队员位置` 和 `移动至使用者指定的位置` 进入新增链路。

## 2026-06-16 20:04 - Phase8 后禁止继续误用旧 Phase7 验收口径

本轮已把失败路线从 live 中移除：

- `0x00EBE9F8` 不再允许指向整张追加表 `0x012ED000`。
- `0x012ED000` 在 Phase8 中变成小扩展 map header，不再是整张 message-map entries。
- `0x012ED020` 才是新增小表，且只能包含 `0xCC/0xCF`。
- 旧按钮 `0xC8/0xC9/0xCA/0xCB` 不应再要求出现在 `0x012ED000` 表里。

后续禁止：

- 用旧 Phase7 脚本逻辑要求 `0x102/0xC8/0xC9/0xCA/0xCB/0xCC/0xCF` 全在 `0x012ED000`。
- 用“new dispatcher 是否包含旧按钮 handler call”判断旧按钮是否保留。
- 把 `0x12ED000` 解析为整表后发现没有旧按钮，就误判 Phase8 失败。

正确检查：

- 主 entries：`0x00EBE9F8 == 0x00FFE1E8`。
- 主 base：`0x00EBE9F4 == 0x012ED000`。
- extension header：`0x012ED000 = { base: 0x00EC21B4, entries: 0x012ED020 }`。
- extension entries：只允许 `0xCC -> 0x012DEF50`、`0xCF -> 0x012DF020`。
- 旧按钮保留证据看原 CSRO runtime initializer 是否仍写：
  - `0xFFE228 -> 0x703F10`
  - `0xFFE250 -> 0x7040E0`
  - `0xFFE278 -> 0x70AB50`
  - `0xFFE2A0 -> 0x7041D0`
## 2026-06-16 20:33 - Phase8 EXE-only 尝试失败，禁止复用

用户复核 GS 文件时间后确认：`D:\CSRO\8-SR_GameServer.exe` 的 LastWriteTime 仍是 `2026-06-16 06:51:01`，说明 Phase8 这一轮没有实际写入 GS。

本轮事实：
- 写前 EXE hash：`4F07FABFEC26C3AAEFEF7BB3B623EEF95306DE6B7A837AEE7076D3AF873E88B8`
- Phase8 写后 EXE hash 曾为：`56C67B783B00CC4F7BBA7015A0C7692FF334C1C8C0A2A98E3EB3A4593C8F1D33`
- GS hash 始终为：`D89E128FECD1BE9FE2E0D67102A407213CFDF9547D65845092BBAAB0EB57373A`
- GS LastWriteTime 始终为：`2026-06-16 06:51:01`
- 因 GS 没有本轮写入，不能称为“EXE/GS 同轮修复”。

失败原因：
- 本轮只扩展了 EXE 侧小 message-map/handler 证据，没有形成 GS/EXE 同轮闭合。
- 把既有 GS 静态检查 4 pass / 0 fail 当成“本轮 GS 已修复”是错误口径；它只能说明 06:51 的 GS handler page 仍存在。
- 任何没有改变 GS hash 或 LastWriteTime 的修复，都不能声称 GS 被本轮修过。

已执行处理：
- 已按本轮备份恢复 `F:\CSRO客户端\SRO_Client.exe`。
- 恢复后 EXE hash 回到：`4F07FABFEC26C3AAEFEF7BB3B623EEF95306DE6B7A837AEE7076D3AF873E88B8`
- 备份目录：`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase8-base-chain-extension-20260616-200403`

后续禁止：
- 禁止再次执行 EXE-only 的 Phase8 写入作为修复轮次。
- 禁止用“GS 静态脚本通过”替代“本轮 GS 写入证据”。
- 禁止在没有同轮备份 EXE 和 GS 的情况下继续修复。
- 禁止在 GS LastWriteTime/hash 不变时说 GS 已修复。
## 2026-06-16 22:13 - Phase9 继续禁用整表替换，并禁止为了时间变化假写 GS

本轮确认 live EXE 的失败根因仍是 `0x00EBE9F8 -> 0x012ED000` 整表替换，旧 CSRO 按钮会被污染。

本轮禁用点：
- 禁止再把 `0x012ED000` 当整张 message-map entries 表。
- 禁止把旧按钮 `0xC8/0xC9/0xCA/0xCB` 放进新增表或接到新 handler。
- 禁止只看 `0x12E39EF`、字符串或静态 handler 存在就宣布成功。
- 禁止为了让 `D:\CSRO\8-SR_GameServer.exe` 的 LastWriteTime 变化而写入无意义字节。

正确边界：
- `0x00EBE9F8` 必须恢复为原 CSRO entries：`0x00FFE1E8`。
- `0x00EBE9F4` 可以指向小扩展 map：`0x012ED000`。
- `0x012ED000` 只能是 `{base=0x00EC21B4, entries=0x012ED020}`。
- `0x012ED020` 只能包含 `0xCC -> 0x012DEF50`、`0xCF -> 0x012DF020` 和 terminator。
- GS 若本轮没有真实缺失点，只能备份并复核链路，不能假称本轮改写了 GS。

## 2026-06-16 22:26 - Phase9 游戏内失败，禁止当成功态继续叠补丁

用户实测红灯：

- 单人状态下 `移动至队员位置` 仍显示为可选外观；正确状态应像 ISRO 一样置灰/不可选。
- CSRO 原有返回功能失效。
- 新增的 `移动至队员位置`、`移动至使用者指定的位置` 点击后客户端闪退。

最新 dump：

- `F:\CSRO客户端\Dump\1N[2026-06-16 22-26-28]_22_34 谁与争锋[西南]_ak48.dmp`
- 异常：`EXCEPTION_ACCESS_VIOLATION`
- 崩溃地址：`0x004B2559`
- 读取地址：`0x0000007C`

当前现场额外问题：

- 当前 GS hash：`260F3F9A55755ACA418718EBD887FABB4043753A44F89EFC12ECF9A187147DEF`
- 相比 Phase9 备份只多 1 字节：raw `0x7EC768` / VA `0x00BEC768`，`_REFSKILL` 被改成 `_REFSKILV`。
- 这不是返回功能链路，不能作为 GS 修复成功证据。

后续禁止：

- 禁止把 Phase9 静态全绿当作成功态。
- 禁止在该失败态上继续叠补丁。
- 禁止忽略单人状态下 `移动至队员位置` 必须不可选的 UI 状态门槛。
- 禁止先修新增按钮，而不先恢复旧 CSRO 返回功能。
- 禁止把新增按钮点击闪退归因给 GS，除非 dump/日志证明已经成功发包并进入服务端处理。

## 2026-06-16 - Phase10 修复前反思：UI 要保留，但旧功能必须先恢复

用户最新确认的三条红灯：

- 当前 UI 形态要保留，但 `移动至队员位置` 在单人状态下没有像 ISRO 一样置灰不可选。
- CSRO 原有返回功能失效。
- 新增两个 ISRO 功能点击会客户端闪退。

本轮新增禁用路线：

- 禁止把“UI 已显示”当成“返回功能已修复”。
- 禁止先修新按钮，必须先恢复旧 CSRO 返回功能。
- 禁止旧按钮 `0xC8/0xC9/0xCA/0xCB` 进入新增 handler。
- 禁止不提取 ISRO 的按钮 enable/disable 状态链路，就只补 `移动至队员位置` 的文字或事件。
- 禁止在没有 dump/日志证据时，把新按钮客户端闪退直接归因给 GS。
- 禁止用 `D:\Files\SR_GameServer.exe` 直接覆盖 live GS；它只能作为同结构参考，失败回滚仍恢复本轮 live 备份。

## 2026-06-16 23:26 - GS 文件锁导致 apply 阻塞，禁止先写 EXE 后写 GS

本轮 Phase10 apply 时，`D:\CSRO\8-SR_GameServer.exe` 被运行中的 `8-SR_GameServer` 进程锁定，PID 为 `42420`。

失败点：

- 脚本第一次尝试先写 EXE，再写 GS。
- GS 写入因文件锁失败。
- 恢复流程尝试恢复 GS 时同样被文件锁阻止。
- 复核后 EXE/GS hash 均仍等于本轮备份，未留下半写入。

已禁用方式：

- 禁止在未确认 EXE/GS 都可写之前开始写入任一目标。
- 禁止 GS 运行时继续尝试写 GS。
- 禁止先改 EXE 再发现 GS 被锁。

已修正：

- `apply_isro_reverse_return_phase10_ui_only_restore.py` 新增写入前可写检查；GS 被锁时会在写入前停止。
