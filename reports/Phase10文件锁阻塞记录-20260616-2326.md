# Phase10 文件锁阻塞记录

记录时间：2026-06-16 23:26

## 本轮目标

执行“只保留 UI、恢复旧 CSRO 返回链路”的 Phase10 第一阶段：

- 保留 EXE 里已经成功显示 ISRO 样式返回 UI 的 bridge。
- EXE active message-map 恢复原 CSRO base/entries，不再主动挂 `0xCC/0xCF` 小扩展功能表。
- GS active client-message filter 恢复为 `D:\Files\SR_GameServer.exe` 同结构参考值。
- 清理 `_REFSKILV` 污染。

## 本轮备份

备份目录：

`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase10-ui-preserve-old-restore-20260616-232001`

备份与 live 当前 hash 一致：

| 文件 | SHA256 |
| --- | --- |
| `F:\CSRO客户端\SRO_Client.exe` | `82961EB58CB8F0BA653D5FA2E6CD188D2EA2821D33E514BC63F42C18875D5DC1` |
| `D:\CSRO\8-SR_GameServer.exe` | `260F3F9A55755ACA418718EBD887FABB4043753A44F89EFC12ECF9A187147DEF` |

## 已生成红灯检查

新增检查脚本：

`C:\Users\43584\Documents\添加ISRO的返回功能\tools\check_reverse_return_phase10_ui_only_restore.py`

当前 live 红灯符合预期：

- UI bridge 保留：pass。
- `0x21 -> 0x12E39EF` UI 入口保留：pass。
- EXE message-map base 仍为 `0x12ED000`：fail，应恢复为 `0x00EC21B4`。
- GS vtable slot 仍为 `0x00E56000`：fail，应按 `D:\Files\SR_GameServer.exe` 恢复为 `0x004C8410`。
- GS 仍含 `_REFSKILV`：fail，应恢复 `_REFSKILL`。

## dry-run manifest

dry-run manifest：

`C:\Users\43584\Documents\添加ISRO的返回功能\evidence\patch-manifest-isro-return-phase10-ui-only-restore-20260616-232459.json`

候选只包含 5 类变更：

1. EXE `0x00EBE9F4`：`0x012ED000 -> 0x00EC21B4`。
2. EXE checksum。
3. GS `0x00BCA560`：`0x00E56000 -> 0x004C8410`。
4. GS `_REFSKILV -> _REFSKILL`。
5. GS checksum。

未做整文件覆盖，未碰 DB/PK2/DLL/其它补丁。

## 第一次 apply 失败

第一次执行 apply 时，脚本先写 EXE，随后写 GS 时失败：

- 错误：`PermissionError: [Errno 13] Permission denied: 'D:\CSRO\8-SR_GameServer.exe'`
- 进一步恢复 GS 时失败：
  - `WinError 32`
  - 另一个程序正在使用此文件。
- 锁定进程：
  - PID：`42420`
  - ProcessName：`8-SR_GameServer`
  - Path：`D:\CSRO\8-SR_GameServer.exe`

复核结果：

- EXE hash 已恢复到本轮备份 hash。
- GS 因本来就未写入，也仍为本轮备份 hash。
- 本次没有留下半写入。

## 已禁用失败方式

已经修改脚本：

`C:\Users\43584\Documents\添加ISRO的返回功能\tools\apply_isro_reverse_return_phase10_ui_only_restore.py`

新增写入前门禁：

- apply 模式下先检查 EXE 是否可写。
- 再检查 GS 是否可写。
- 只要任一目标被锁定，直接停止，不写任何目标文件。

第二次 apply 验证：

- 在写入前因为 GS 锁定直接失败。
- EXE/GS hash 均保持与备份一致。

## 下次继续前必须满足

1. 先停止 `D:\CSRO\8-SR_GameServer.exe` 对应进程。
2. 再执行 Phase10 apply。
3. 如果 apply 后静态检查失败，立即恢复本轮备份目录中的 EXE 和 GS。
4. 禁止在 GS 正在运行/文件被锁时继续尝试写入。

## 结论

本轮没有完成目标文件修复，阻塞原因是 GS 文件被运行进程锁定。补丁候选已经准备好，且脚本已修正为“写前锁检查”，避免再次出现先写 EXE 后写 GS 失败的半写入风险。
