# Phase10 执行记录：保留 UI，恢复旧 CSRO 返回功能

记录时间：2026-06-16 23:40

## 本轮目标

- 保留当前已经成功出现的 ISRO 风格返回 UI。
- 去掉前几轮失败补丁中会破坏旧 CSRO 返回功能的 active 链路。
- 只恢复返回相关补丁，其它补丁不动。
- GS 按同版本同结构参考 `D:\Files\SR_GameServer.exe` 只恢复返回相关 active slot，不整文件覆盖。

本轮不宣布完成：

- `移动至队员位置`
- `移动至使用者指定的位置`

这两个新增功能的完整 GS/EXE 闭环留到下一阶段处理。

## 备份

备份目录：

`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase10-ui-preserve-old-restore-20260616-232001`

写入前确认：

| 项目 | SHA256 |
| --- | --- |
| live EXE | `82961EB58CB8F0BA653D5FA2E6CD188D2EA2821D33E514BC63F42C18875D5DC1` |
| backup EXE | `82961EB58CB8F0BA653D5FA2E6CD188D2EA2821D33E514BC63F42C18875D5DC1` |
| live GS | `260F3F9A55755ACA418718EBD887FABB4043753A44F89EFC12ECF9A187147DEF` |
| backup GS | `260F3F9A55755ACA418718EBD887FABB4043753A44F89EFC12ECF9A187147DEF` |

## 写入 manifest

`C:\Users\43584\Documents\添加ISRO的返回功能\evidence\patch-manifest-isro-return-phase10-ui-only-restore-20260616-234022.json`

写入结果：

| 文件 | 写入后 SHA256 | LastWriteTime |
| --- | --- | --- |
| `F:\CSRO客户端\SRO_Client.exe` | `2311872E2C7825684EF5EA6D10BCBBE8792DEE98AD6FCF92005B26CCDCA33E3F` | `2026-06-16 23:40:22` |
| `D:\CSRO\8-SR_GameServer.exe` | `99CA6B6D9D7EB7C7B85C14D750DFAAC01C98430D2FC2FC325BD053CF8FB2D157` | `2026-06-16 23:40:22` |

## 本轮实际写入字节

| 目标 | VA | raw offset | old bytes | new bytes | 分类 |
| --- | --- | --- | --- | --- | --- |
| EXE | `0x00EBE9F4` | `0xABE9F4` | `00 D0 2E 01` | `B4 21 EC 00` | `restore_old_csro` |
| EXE | checksum | `0x180` | `EE E5 C7 00` | `60 37 C7 00` | `checksum` |
| GS | `0x00BCA560` | `0x7CA560` | `00 60 E5 00` | `10 84 4C 00` | `restore_gs_reference` |
| GS | string cleanup | `0x7EC760` | `_REFSKILV` | `_REFSKILL` | `cleanup_failed_patch_pollution` |
| GS | checksum | `0x138` | `C4 C0 98 00` | `3C E4 97 00` | `checksum` |

明确未做：

- 未整表替换 message-map。
- 未把旧按钮 `0xC8/0xC9/0xCA/0xCB` 接到新 handler。
- 未删除 EXE 里 inert 的 `0x012ED000` 扩展字节。
- 未删除 GS 里 inert 的追加 handler/扩展段。
- 未整文件覆盖 GS。
- 未动其它补丁。

## 静态检查结果

### Phase10 主检查

命令：

`python .\tools\check_reverse_return_phase10_ui_only_restore.py --json`

结果：`7 pass / 0 fail`

通过含义：

- UI bridge 保留。
- UI bridge 仍保留 big dispatch 和旧 fallback。
- 真实右键入口 `0x21` 仍进入 `0x12E39EF`，ISRO 风格 UI 入口保留。
- active message-map base 已恢复到原 CSRO `0x00EC21B4`。
- active message-map entries 为原 CSRO `0x00FFE1E8`。
- GS client-message filter slot 已恢复为 `D:\Files\SR_GameServer.exe` 同结构参考值 `0x004C8410`。
- GS 中 `_REFSKILV` 污染已清除，恢复为 `_REFSKILL`。

### 旧 CSRO 按钮保留检查

命令：

`python .\tools\check_reverse_return_phase8_old_csro_preserved.py --json`

结果：`11 pass / 2 fail`

这两个 fail 是旧 Phase8 脚本期望导致：

- 旧脚本要求 GS hash 仍是更早的 `D89E...`，但本轮已按 Phase10 写入 GS 恢复补丁，hash 必然变化。
- 旧脚本要求 `main_message_map_base_is_phase8_extension == 0x12ED000`，但本轮目标就是把 active base 从失败的 `0x12ED000` 恢复到原 CSRO `0x00EC21B4`。

旧按钮关键项为通过：

- `0xC8 -> 0x703F10`
- `0xC9 -> 0x7040E0`
- `0xCA -> 0x70AB50`
- `0xCB -> 0x7041D0`

并且旧按钮不在扩展表中，没有接入新 handler。

### 深链路检查

命令：

`python .\tools\deep_reverse_chain_check.py --targets --json`

结果：`8 pass / 3 fail`

EXE 新功能静态链路仍可见，但 GS 新功能路由失败：

- `gs_final_move_protocol_routes`
- `gs_list_request_response_protocol_routes`
- `gs_party_position_requires_party_size_at_least_two`

解释：

这是 Phase10 的预期状态。因为本轮先恢复旧 CSRO 返回链路，已将 GS active slot 从失败的新 handler `0x00E56000` 恢复到同结构 CSRO 参考 `0x004C8410`。所以新增的 `0x3213/0x705A/0x759F/0x7600/B59F/B600` 完整新功能链路现在不是 active 状态，下一阶段必须重新一一对比 ISRO GS/EXE 后，只给 `0xCC/0xCF` 补完整链路。

## Dump / FatalLog 当前状态

当前未进行游戏内实测，因此不能用“无新 dump”证明成功。

检查到 `F:\CSRO客户端\Dump` 最新 dump 仍是旧失败文件：

`F:\CSRO客户端\Dump\1N[2026-06-16 22-26-28]_22_34 谁与争锋[西南]_ak48.dmp`

`D:\CSRO\FatalLog` 目录当前不存在；`D:\CSRO\Log` 存在。

## 游戏内验收清单

必须人工进游戏验证，静态通过不算最终成功：

1. 第一次右键返回卷轴：不闪退，ISRO 风格 UI 仍出现。
2. 关闭后第二次右键返回卷轴：不闪退，UI 仍正常。
3. CSRO 原有返回功能全部可用。
4. 单人状态下 `移动至队员位置` 应像 ISRO 一样灰色/不可选。
5. 点击旧 CSRO 返回按钮不得进入新 handler 导致失效。
6. 验证同一时间段 `F:\CSRO客户端\Dump` 没有新 dump。
7. 验证 `D:\CSRO\Log` 或实际 FatalLog 位置没有新增异常。

## 失败回退

如果游戏内验证失败，必须直接从本轮备份恢复 EXE 和 GS，不手工换字节，不在失败状态继续叠补丁：

`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-isro-return-phase10-ui-preserve-old-restore-20260616-232001`

恢复目标：

- `F:\CSRO客户端\SRO_Client.exe`
- `D:\CSRO\8-SR_GameServer.exe`

## 下一阶段注意

下一阶段若继续处理 `0xCC/0xCF` 新功能，必须：

- 以真实入口 `0x21` 和当前 UI bridge 为准。
- 旧按钮 `0xC8/0xC9/0xCA/0xCB` 继续保持原 CSRO 链路。
- 只给 `0xCC/0xCF` 建新链路。
- 必须重新对比 ISRO GS `F:\SR_GameServer.exe` 与 ISRO EXE `F:\ISRO客户端国服\sro_client.exe`。
- 必须提取队伍人数门槛，让单人状态 `移动至队员位置` 灰色/不可选。
- 禁止再走整表替换、旧入口误判、只看静态通过这三条旧失败路线。
