# Phase1 游戏内失败复盘：单人队员按钮置灰无变化

时间：2026-06-17

## 现象

用户游戏内实测：单人未组队状态下右键返回卷轴，`移动至队员位置` 仍然是亮色可选，没有变成 ISRO 单人状态下的灰色不可选。

## 本轮失败补丁

Phase1 只恢复了 EXE 中队员按钮 enable 判断的 call：

- 位置：`F:\CSRO客户端\SRO_Client.exe` raw `0x00C66BC6` / VA `0x012E3BC6`
- 写入前：`E8 F5 8D 00 00`，即 Phase11A 的 `state_global+0x30 >= 2` gate stub
- 写入后：`E8 85 82 79 FF`，即 call `0x00A7BE50`
- GS 未写入

## 游戏内结论

静态上 CSRO 该块与 ISRO UI enable 块同构，但游戏内没有变化。因此：

- “只把 enable call 恢复到 `0x00A7BE50`”不是充分修复。
- “静态同构 + 字节存在 + checksum 更新”不能证明置灰成功。
- Phase11A 的 `state_global+0x30 >= 2` gate 已失败；Phase1 的“只恢复 call 目标”也已失败。

## 已执行回滚

按铁律恢复本轮备份双文件：

- 备份目录：`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-phase1-isro-party-flag-restore-20260617-084047`
- 回滚后 EXE SHA256：`BE9D5D0D62CC931F71B80ACEC88FBB2F297B78BF6C712A57D4A80E286615119C`
- 回滚后 GS SHA256：`B5C56F6A92B317C1701E1CF9E963754E4A0A52FFA74ACEFD91BC1E1DF8915C98`

## 新增禁用路线

后续禁止复用以下方法：

1. 只恢复 `0x012E3BC6 -> 0x00A7BE50`，然后认为队员按钮置灰已修复。
2. 只看 `state_global+0x2E` 读取函数存在或与 ISRO 同构，就认为运行时状态正确。
3. 没有动态证据就写新的置灰补丁。
4. 未验证第一次、第二次右键和旧 CSRO 返回功能，就继续推进新功能。

## 下一步只读定位方向

真正要追的是运行时状态来源：

- `0x00A7BE50` 实际读取 `state_global+0x2E`。
- 需要确认单人打开返回 UI 时，CSRO 的 `state_global+0x2E` 为什么仍返回可用。
- 重点只读追踪 `state_global+0x2E` 的写入链路、party packet/refresh 链路、UI 打开前后的运行时值。
- 没有拿到运行时值或写入来源证据前，不写 EXE/GS。