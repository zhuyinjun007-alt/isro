# Phase2 执行记录：队员置灰改用列表计数 gate

时间：2026-06-17 09:18

## 目标

只处理单人未组队时 `移动至队员位置` 必须置灰不可选的问题。保持 ISRO 五按钮 UI，保持旧 CSRO 返回功能 active 链路，不处理 `0xCC/0xCF` 完整新功能传送链路。

## 修复前结论

Phase11A 的 `state_global+0x30 >= 2` gate 已被游戏内实测证明无效；Phase1 的 `0x012E3BC6 -> 0x00A7BE50` 也被游戏内实测证明无变化。

本轮重新对比 CSRO/ISRO 状态对象：

- CSRO reset：`0x00A8E180`
- ISRO reset：`0x00AECAB0`
- 两边都把 `+0x44` 当作队员列表计数：reset 先判断 `[state+0x44]`，遍历 `[state+0x40]` 链表，删除节点后执行 `[state+0x44] -= 1`。
- 两边都在 reset 末尾清 `[state+0x30] = 0`、`[state+0x2E] = 0`。

所以 `+0x30` 不是队伍人数；Phase11A 失败路线必须禁用。

## 写入内容

正式备份目录：

`C:\Users\43584\Documents\添加ISRO的返回功能\backups\pre-phase2-party-list-count-gate-20260617-091800`

Manifest：

`C:\Users\43584\Documents\添加ISRO的返回功能\evidence\patch-manifest-phase2-party-list-count-gate-20260617-091800.json`

只写 EXE，GS 只备份不写：

| 文件 | VA | raw | old | new | 说明 |
| --- | --- | --- | --- | --- | --- |
| EXE | `0x012EC9C0` | `0x00C6F9C0` | `83 79 30 02 0F 9D C0 C3` | `83 79 44 00 0F 97 C0 C3` | 改为 `cmp [ecx+0x44],0; seta al; ret` |
| EXE | checksum | `0x180` | `EF 09 C8 00` | `03 02 C8 00` | 更新 PE checksum |

`0x012E3BC6` 保持 call 到 `0x012EC9C0`，只影响 `0xCC` 队员按钮 enable 判断。

## 验证结果

静态 postcheck：`5 pass / 0 fail`

Phase10 基线检查：`7 pass / 0 fail`

当前文件 hash：

- EXE：`280C277CF73E93DBA9F384722653BD630CCA0EA8531EBCA387D792FAB41DB70E`
- GS：`B5C56F6A92B317C1701E1CF9E963754E4A0A52FFA74ACEFD91BC1E1DF8915C98`

GS hash 和 LastWriteTime 未变化，说明本轮没有写 GS；`_REFSKILV` 用户补丁保留。

## 必须游戏内验证

静态通过不算最终成功。必须验证：

1. 第一次右键返回卷轴，UI 正常出现。
2. 第二次右键返回卷轴，UI 仍正常出现，不闪退。
3. 单人未组队时 `移动至队员位置` 为灰色不可选。
4. 原 CSRO 返回功能仍可用。
5. 无新增 dump/FatalLog。

若游戏内失败，必须直接恢复本轮备份目录中的 EXE 和 GS，不手工换补丁。